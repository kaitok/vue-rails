vue-rails
==============

Vue.js + Rails

Install npm package

    $ npm i

Install gem package

    $ bundle install

Start the server and browse <http://localhost:3000/> to check it's working

    $ rails s
